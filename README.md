# Running the project
Clone the rep
`git clone https://github.com/RaissaOliv/pokedex.git`<br/>
access the project archive<br/>
`cd pokedex`<br/>
install the deps<br/>
`npm install`
run the project<br/>
`npm start`
