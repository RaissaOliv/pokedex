# Running the project
Clone the rep
`git clone https://github.com/RaissaOliv/pokedex.git`<br/>
access the project archive\n
`cd pokedex`<br/>
install the deps\n
`npm install`
run the project\n
`npm start`
